# mediamtx configuration file

rtsp: yes
protocols: [tcp, udp]

rtmp: no
hls: no
webrtc: no
srt: no

logLevel: info

# if USB deconnects and /dev/video0 isn't available: `sudo usbreset "USB2.1 Hub"`
paths:
  ch0_0:
    runOnInit: >
      ffmpeg -loglevel error -f v4l2 -input_format mjpeg -video_size 1920x1080 -framerate 10 -i /dev/video0
        -f rtsp -rtsp_transport tcp -c:v copy -c:a none rtsp://localhost:8554/ch0_0
    runOnInitRestart: yes
    runOnReady: ffmpeg -hide_banner -loglevel error -y -i rtsp://localhost:8554/ch0_0 -vf fps=1 -update 1 /usr/local/www/ch0_0.jpg
    runOnReadyRestart: yes
